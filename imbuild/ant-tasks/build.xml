
<project name="ant-tasks" default="default" basedir=".">
  <description>build, test, package build tasks</description>
  <import file="../library.xml"/>
  
  <target name="-post-jar">
    <copy file="${main.dist.jar}" toDir="../lib"/>
  </target>
  
</project>

