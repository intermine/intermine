compile.dependencies = bio/core/main

# This dependency lives here because it causes the properties.file to be
# set, which screws up webapp deployments. Do not add it to compile.dependencies
extra.project.dependencies = bio/test-all/dbmodel
