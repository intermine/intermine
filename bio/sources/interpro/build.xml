<project name="interpro" default="default" basedir=".">
    <description>build, test, package interpro</description>

    <import file="../../../imbuild/source.xml"/>

    <property name="interpro.src.alias.token" value="##INTERPRO_SRC_ALIAS##"/>
    <property name="interpro.organism.taxid.list.token" value="##ORGANISM_TAXID_LIST##"/>

<!--
    <target name="-pre-retrieve">

        <delete dir="${build.dir}/sql"/>
        <mkdir dir="${build.dir}/sql"/>

        <copy file="${interpro.summary.sql.file}" tofile="${build.dir}/sql/buildInterproSummaryFromInterpro12.sql"/>

        <replace file="${build.dir}/sql/buildInterproSummaryFromInterpro12.sql" token="${interpro.src.alias.token}" value="${interpro.src.alias}"/>
        <replace file="${build.dir}/sql/buildInterproSummaryFromInterpro12.sql" token="${interpro.organism.taxid.list.token}" value="${interpro.organism.taxid.list}"/>

        <sql
            classpathref="task.class.path"
            driver="org.postgresql.Driver"
            url="jdbc:postgresql://gollum:5432/interpro"
            userid="flymine"
            password="flymine"
            src="${build.dir}/sql/buildInterproSummaryFromInterpro12.sql"
        />

    </target>
-->

</project>

