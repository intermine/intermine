# Specify keys for classes in the data model.  Used to:
#
#  * define which types can be stored in 'object' lists
#  * define which fields in the webapp are links in results pages
#  * porting lists between production databases

BioEntity = primaryIdentifier, secondaryIdentifier
Gene = primaryIdentifier
Protein = primaryAccession
OntologyTerm = identifier, name
Organism = name, shortName, taxonid
Transcript = primaryIdentifier
Publication = pubMedId
Author = name
DataSource = name
DataSet = name
