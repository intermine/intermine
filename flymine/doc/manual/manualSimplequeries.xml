<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD Simplified DocBook XML V1.0//EN"
          "http://www.oasis-open.org/docbook/xml/simple/1.0/sdocbook.dtd">
<article id="manualSimplequeries">
  <section>
    <title>Building simple queries</title>

    <itemizedlist>
      <listitem>
        <para>Building a simple query using the FlyMine query builder involves
        the following steps:</para>
      </listitem>

      <listitem>
        <para>Select the class from which you want to start your query, from
        the <ulink url="manualClasschooser.xml">Class chooser.</ulink></para>
      </listitem>

      <listitem>
        <para>Select the classes and /or fields you wish to constrain using
        the -&gt; constrain arrows. Specify your constraints in the right hand
        <ulink url="manualConstraintslist.xml">constraints
        list.</ulink></para>
      </listitem>

      <listitem>
        <para>Select the classes and/or fields you wish to show in the results
        output using the 'show' buttons. The classes/fields selected will
        appear in the <ulink url="manualOutputfields.xml">output fields
        list.</ulink></para>
      </listitem>

      <listitem>
        <para>Press <ulink url="manualShowresults.xml">show results</ulink> to
        view your results in the <ulink url="manualResultdisplay.xml">results
        page</ulink></para>
      </listitem>
    </itemizedlist>
  </section>

  <section>
    <title>Constraints</title>

    <para>To select the subset of objects you want from a particular class you
    apply a constraint to the appropriate field. For example, if you want
    objects from the Gene class which have a particular name, you constrain
    the gene name field. If you are constraining a class field you will be
    given the option to constrain that class to any of the subclasses of that
    class. e.g. The transcript class can be constrained to a number of
    subclasses of Transcript (e.g. primary transcript, processed transcript,
    tRNA etc).</para>
  </section>

  <section>
    <title>Fields for output</title>

    <para>The fields you wish to display in the output must be selected from
    the model browser. If any of the fields selected do not contain</para>
  </section>
</article>
