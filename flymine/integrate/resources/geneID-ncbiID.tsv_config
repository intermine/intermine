# the name of the class that will have it's fields set
className: org.flymine.model.genomic.Gene

# the column in the file to use as the primary key when finding objects to change
keyColumn: 0

# the name of the fields that correspond to each column
column.0: identifier
column.1: ncbiGeneNumber
