language: java
env:
    - TEST_SUITE=model
    - TEST_SUITE=objectstore
    - TEST_SUITE=integrate
    - TEST_SUITE=pathquery
    - TEST_SUITE=api
    - TEST_SUITE=web
    - TEST_SUITE=bio
    - TEST_SUITE=selenium
    - TEST_SUITE=checkstyle
sudo: false # use container based infrastructure
jdk:
#    - oraclejdk7
    - openjdk7
    - openjdk6
before_script:
    - ./config/travis.init.sh
before_install:
    - "export DISPLAY=:99.0"
    - ./config/travis-before-install.sh
script:
    - ./config/travis-run.sh
addons:
    sauce_connect:
        username: "ajk59"
        access_key: "28fedb2c-35b4-4bbc-b4f3-735ad303ed71"
notifications:
  irc:
      channels:
          - "chat.freenode.net#intermine-ci"
      on_success: change
      on_error: change

