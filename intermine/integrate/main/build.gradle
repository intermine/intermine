sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
        resources {
            srcDirs = ['resources']
        }
    }
}

dependencies {
  compile project(':intermine-objectstore-main')
  compile project(':zfulldata')
  compile group: 'stax', name: 'stax', version: '1.2.0' //stax-ri 1.0 not available on maven 
}
