To build the code, simply execute the build script:

> ./build

To run the compiled code execute the run script

> ./run

To build the site documentation, execute:

mvn site:stage


The code will build with any version of Maven2. The site documentation requires
at least Maven 2.1.x.

