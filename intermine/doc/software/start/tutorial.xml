<?xml version="1.0"?>
<article>
  <artheader>
    <title>How to build and run the tutorial</title>
  </artheader>
  <sect1>
    <para>The tutorial consists of a simple model in ArgoUML format, some test data for this model and a Java program to query the data. It includes the InterMine jar, its dependencies, the necessary configuration files and a build script.</para>
    <para>Building the tutorial involves a few simple steps:</para>
    <para>
      <orderedlist>
        <listitem>Ensure that you have the <ulink url="prerequisites.xml">required software</ulink> installed, including Tomcat.</listitem>
        <listitem><ulink url="http://download.flymine.org/nightly/">Download</ulink> and unpack the latest InterMine tutorial (intermine-tutorial-xxxxxxxx).</listitem>
        <listitem>Change to the tutorial root directory, modify the <filename>intermine.properties</filename> configuration file to reflect your database server name, database user name, and password if necessary. Note that you must run <command>createdb tutorial</command> before attempting to store data.</listitem>
        <listitem>Depending on your requirements there are a choice of targets that you can run. Issue the command <command> ant help</command> to display user targets or <command>ant -projecthelp</command> for a full target list. <command>ant run</command> will read and store the data, and perform a query to ensure it was stored correctly. The source (<filename>src/java/org/intermine/tutorial/SimpleQuery.java</filename>) demonstrates how this query is run for the model (<filename>model/tutorial/tutorial.zargo</filename>); the results, which can be compared to the test data (<filename>resources/tutorial_data.xml</filename>).</listitem>
        <listitem>If you wish to deploy the web front-end for the tutorial model then first modify the <filename>build.properties</filename> configuration file ensuring the destination server and path are correct. Then run the <command>ant release-webapp</command> command to release the tutorial web application to the server. </listitem>
      </orderedlist>
    </para>
  </sect1>
</article>
